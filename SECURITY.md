# 安全策略

## 支持的版本

仅最新发布版本会收到安全修复。

| 版本 | 支持状态 |
|---|---|
| 1.8.x | ✅ 支持 |
| < 1.8 | ❌ 不再支持 |

## 报告漏洞

如果你发现了安全漏洞，请**不要**直接开公开 Issue，改用以下任一方式：

1. 通过 GitHub 的 [Security Advisories](../../security/advisories/new) 私密上报；
2. 若无法使用上述通道，可开一个不含复现细节的 Issue，并注明"需要私密沟通"。

我们会尽快确认并回复，修复后再公开披露细节。

## 凭据与密钥说明

- 仓库中**不包含**任何签名密钥（`*.keystore` 已被 `.gitignore` 屏蔽）。
- 本地构建所需的口令放在 `local.properties`（该文件不入库）；
  CI 使用现场随机生成的一次性密钥，口令只存在于运行时环境变量中。
- 请勿在 Issue、PR 或截图中附带完整的登录 Cookie、`authkey`、`stoken` 等敏感信息。

## 应用侧说明

本应用仅在本机保存登录态与抽卡记录，用于本地统计展示；请妥善保管个人设备与账号。
